/**
 * Центральные переключатели UI. Верните `true`, когда блоки снова понадобятся в интерфейсе.
 */
export const uiFlags = {
  /**
   * ВРЕМЕННО `false`: можно открывать страницы кабинета по прямой ссылке без входа.
   * Где отключено:
   * - `App.tsx` — `ProtectedRoute` не редиректит на `/?next=…`
   * - `LandingPage.tsx` — не открывается модалка входа при `?next=…`
   * - `Header.tsx` — на маршрутах кабинета показывается полная шапка (навигация), как для вошедшего
   * Вернуть `true`, когда логин/регистрация стабильны и нужна защита маршрутов.
   */
  cabinetRoutesRequireAuth: false,

  /** Модалка уведомлений по иконке колокольчика в шапке */
  notificationsPanelEnabled: true,
  /** Модалка отчёта, встроенный `ReportContent`, кнопки «Открыть отчёт» / «Открыть» */
  reportViewsEnabled: true,
} as const;
