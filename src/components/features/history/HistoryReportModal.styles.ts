/**
 * Для модалки отчёта убираем «карточку» ModalShell:
 * фон, рамку и внутренние отступы — чтобы остался один визуальный контейнер отчёта.
 */
export const historyReportModalPanelStyles =
  'flex w-full flex-col !bg-transparent !border-0 !shadow-none !p-0';
